<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://java.sun.com/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://java.sun.com/xml/ns/javaee
                             http://java.sun.com/xml/ns/javaee/web-app_2_5.xsd"
         version="2.5">

    <display-name>Struts 1 Demo</display-name>

    <!-- ============================================== -->
    <!-- Authentication Filter Configuration           -->
    <!-- Protects /atm/* and /admin/* paths            -->
    <!-- ============================================== -->
    <filter>
        <filter-name>AuthenticationFilter</filter-name>
        <filter-class>com.demo.security.AuthenticationFilter</filter-class>
    </filter>

    <!-- Filter mapping for ATM operations (require login) -->
    <filter-mapping>
        <filter-name>AuthenticationFilter</filter-name>
        <url-pattern>/atm/*</url-pattern>
    </filter-mapping>

    <!-- Filter mapping for Admin operations (require login) -->
    <filter-mapping>
        <filter-name>AuthenticationFilter</filter-name>
        <url-pattern>/admin/*</url-pattern>
    </filter-mapping>

   <servlet>
	<servlet-name>action</servlet-name>
		<servlet-class>org.apache.struts.action.ActionServlet</servlet-class>
	<init-param>
		<param-name>config</param-name>
		<param-value>/WEB-INF/struts-config.xml</param-value>
	</init-param>
	<init-param>
		  <param-name>debug</param-name> 
		  <param-value>2</param-value> 
	  </init-param>
	  <init-param>
		  <param-name>detail</param-name> 
		  <param-value>2</param-value> 
	  </init-param>
	<load-on-startup>2</load-on-startup>
</servlet>

    <servlet-mapping>
        <servlet-name>action</servlet-name>
        <url-pattern>*.do</url-pattern>
    </servlet-mapping>
    
	    

    

    <welcome-file-list>
        <welcome-file>test.do</welcome-file>
    </welcome-file-list> 
</web-app>

